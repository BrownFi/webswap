// Shim for `@reown/appkit/networks` — only referenced by the standalone-app
// config (now unreachable in the webswap integration). Kept so any stray import
// resolves to a harmless type.
export type AppKitNetwork = any
